@model Resturant_System.Models.Meal_Category

@{
    ViewBag.Title = "Create";
}

<h3 class="h3">Create new Meal Category</h3>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset >
        <legend> </legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Name , new {  @Class = "label label-primary" })
        </div>
        <div class="editor-field  ">
            @Html.EditorFor(model => model.Name , new {  @Class = "field form-control" })
            @Html.ValidationMessageFor(model => model.Name)
        </div>
        @Html.HiddenFor(model => model.Image, new { @Value = "/images/main/No_image_available.jpg" })
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
